import type{MotionManifest,StartFrameRegistryEntry}from"../contracts"
export function resolveMotion(frame:StartFrameRegistryEntry,manifests:MotionManifest[]){const manifest=manifests.find(m=>m.frameId===frame.frameId&&m.shotId===frame.shotId);if(!manifest)return{error:"MOTION_MANIFEST_MISSING"as const};if(frame.status!=="APPROVED")return{error:"NEEDS_START_FRAME_APPROVAL"as const};const actions=[manifest.primaryAction,...manifest.allowedMotion.filter(x=>!manifest.primaryAction.toLowerCase().includes(x.toLowerCase()))];if(actions.length>3||manifest.forbidden.includes("multiple-actions-requested"))return{error:"ACTION_OVERLOAD"as const};if(manifest.durationSeconds<=0)return{error:"INVALID_DURATION"as const};return{manifest}}
export function motionClass(m:MotionManifest){const text=[m.primaryAction,m.cameraMotion,...m.allowedMotion].join(" ").toLowerCase();if(text.includes("hand"))return"handMotion";if(text.includes("push"))return"cameraPushIn";if(text.includes("hold"))return"singleActionClips";return"singleActionClips"}
