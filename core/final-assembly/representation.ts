import type{AssemblyBlockingError,FinalAssemblyInput}from"../contracts/final-assembly"
export function verifyRepresentation(i:FinalAssemblyInput){const errors:AssemblyBlockingError[]=[];for(const s of i.editBlueprint.shots){const r=i.representationRules.find(x=>x.representationType===s.representationType);if(!r)errors.push("ASSEMBLY_INPUT_INVALID");if((s.labelRequired||r?.labelRequired)&&(!r?.labelText||!r.legible||!r.safeAreaValid))errors.push("REPRESENTATION_LABEL_MISSING");for(const id of s.evidenceIds){const e=i.evidenceMatrix.find(x=>x.evidenceId===id&&x.shotId===s.shotId);if(!e?.verified||!e.assetHash||e.classification!==s.representationType)errors.push("EVIDENCE_INTEGRITY_FAILED")}}return[...new Set(errors)]}
