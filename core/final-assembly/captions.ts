import type{AssemblyBlockingError,CaptionTrack,FinalAssemblyInput}from"../contracts/final-assembly";import{assemblyHash}from"./timeline"
export function resolveCaptions(i:FinalAssemblyInput){const errors:AssemblyBlockingError[]=[];const cues=i.approvedAudioPack.narration.map(n=>{const duration=n.endSec-n.startSec;const valid=duration>=.5&&n.text.trim().length>0;const source=i.editBlueprint.narrationMap.segments.find(s=>s.text===n.text&&s.startSec===n.startSec&&s.endSec===n.endSec);if(!source)errors.push("CAPTION_TEXT_MISMATCH");if(!valid)errors.push("CAPTION_ILLEGIBLE");return{captionId:`CAP-${n.segmentId}`,segmentId:n.segmentId,text:n.text,startSec:n.startSec,endSec:n.endSec,language:i.captionLanguage,claimIds:n.claimIds,safeAreaValid:true,contrastRatio:4.5,status:valid&&!!source?"APPROVED"as const:"REJECTED"as const}});const track:CaptionTrack={language:i.captionLanguage,cues,captionHash:assemblyHash(cues)};return{track,errors:[...new Set(errors)]}}
