import type{AgentTask,FinalAssemblyInput}from"../../core/contracts";import{AgentResultSchema}from"../../core/schemas";import type{Agent}from"../../core/interfaces";import{runFinalAssembly,type FinalAssemblyAdapter}from"../../core";import{MockFinalAssemblyAdapter}from"../../providers/assembly"
export class FinalAssemblyAgent implements Agent{readonly id="final-assembly";constructor(private readonly adapter:FinalAssemblyAdapter=new MockFinalAssemblyAdapter()){}async execute(task:AgentTask){const pack=runFinalAssembly(task.input as FinalAssemblyInput,this.adapter),now=new Date().toISOString(),approved=pack.status==="DELIVERY_APPROVED";return AgentResultSchema.parse({schemaVersion:task.schemaVersion,projectId:task.projectId,runId:task.runId,status:approved?"completed":"failed",warnings:pack.warnings,blockingErrors:pack.blockingErrors,createdAt:now,updatedAt:now,sourceVersions:[{source:this.id,version:"1.0.0"}],artifactIds:approved?[pack.finalRender.renderId]:[],checkpointRef:null,nextAgent:approved?"packaging-agent":null,id:`result-${task.id}`,taskId:task.id,agentId:this.id,requestedTransition:approved?"MASTER_APPROVED":null,output:pack})}}
