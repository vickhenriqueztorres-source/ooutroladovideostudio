import type{AgentTask,ProviderValidationInput}from"../../core/contracts";import{AgentResultSchema}from"../../core/schemas";import type{Agent}from"../../core/interfaces";import{validateProviderCapabilities}from"../../core/provider-validation"
export class ProviderCapabilityValidationAgent implements Agent{readonly id="provider-capability-validation";async execute(task:AgentTask){const result=validateProviderCapabilities(task.input as ProviderValidationInput),now=new Date().toISOString();return AgentResultSchema.parse({schemaVersion:task.schemaVersion,projectId:task.projectId,runId:task.runId,status:result.status==="READY"||result.status==="READY_WITH_WARNINGS"?"completed":"failed",warnings:result.warnings,blockingErrors:result.blockingErrors,createdAt:now,updatedAt:now,sourceVersions:[{source:this.id,version:"1.0.0"}],artifactIds:[],checkpointRef:null,nextAgent:result.providerExecutionPlan?"provider-job-orchestrator":null,id:`result-${task.id}`,taskId:task.id,agentId:this.id,requestedTransition:result.providerExecutionPlan?"PROVIDER_PLAN_APPROVED":null,output:result})}}
